# Copy this file to keystore.properties and fill it on the release machine.
# keystore.properties and keystore files are intentionally ignored by git.

storeFile=release-signing.p12
storeType=PKCS12
storePassword=change-me
keyAlias=legado-tauri-release
keyPassword=change-me
